# Pokemom_Type_Matchup
**Pokémon_Type_Matchup** is a **JavaFX desktop application** that lets users select a Pokémon type and instantly view its **type effectiveness matchups**—showing which types it hits for **2× damage**, which it hits for **0.5×**, and which it has **no effect** on—using a simple, visual interface with themed icons and backgrounds.
